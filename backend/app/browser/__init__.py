"""The session vault: persistent, per-portal Playwright browser profiles.

See `session.py`. Nothing here — or anywhere under this package — ever accepts
or stores a credential. See spec §6.3 and `backend/tests/test_no_autosubmit.py`.
"""
